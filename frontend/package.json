{
  "name": "poketrade-frontend",
  "version": "1.0.0",
  "description": "Frontend de PokeTrade — TFG DAW (vanilla JS, módulos ES6)",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "node --experimental-vm-modules node_modules/jest/bin/jest.js"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  },
  "jest": {
    "testEnvironment": "node",
    "testMatch": ["**/tests/**/*.test.js"]
  }
}
