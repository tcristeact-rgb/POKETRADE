{
  "name": "poketrade-tests-e2e",
  "private": true,
  "description": "Suite mínima de Playwright: los caminos críticos de PokeTrade contra la API y el frontend locales.",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "instalar-navegador": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.0"
  }
}
